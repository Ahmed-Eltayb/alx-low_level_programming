#include"main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @int: number input
 *
 * Return: Returns 0
 */
int _abs(int)
{
	int n;

	if (n >= 0)
	{
		n = n;
		_putchar('n');
	}
	else
	{
		n = n * -1;
		_putchar('n');
	}
	return (0);
}
